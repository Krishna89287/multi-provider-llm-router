# Optional. Set either of these to route real calls to that provider instead of
# the offline mock. Without them the router still runs, using mock responses.
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
